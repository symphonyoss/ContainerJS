{
  "name": "containerjs-api-openfin",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && copyfiles -f src/notification.html dist && rollup -c",
    "test": "npm run build",
    "postinstall": "npm run build",
    "test:ui": "exit 0"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "copyfiles": "^1.2.0",
    "rimraf": "^2.6.1",
    "rollup": "^0.41.6"
  }
}
